<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:orientation="vertical" >

    <View
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:background="@color/pp_conversations_item_divider" >
    </View>

    <Button
        android:id="@+id/pp_create_conversation_btn"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/pp_create_conversations_btn_height"
        android:layout_gravity="center"
        android:layout_margin="10dp"
        android:background="@drawable/pp_create_conversations_btn_bg"
        android:padding="5dp"
        android:text="@string/pp_create_conversation"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        android:textSize="16sp" />

</LinearLayout>