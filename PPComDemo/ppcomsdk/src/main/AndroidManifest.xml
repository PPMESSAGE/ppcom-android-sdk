<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ppmessage.ppcomlib">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity
            android:name=".ui.PPComMessageActivity"
            android:parentActivityName=".ui.ConversationsActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.ConversationsActivity"
                ></meta-data>
        </activity>

        <activity android:name=".ui.ConversationMemberActivity"
            android:parentActivityName=".ui.PPComMessageActivity" >

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.PPComMessageActivity"
                ></meta-data>

        </activity>

    </application>

</manifest>
